# TeqFW built-in web server runtime configuration.
TEQFW_WEB__PORT=3000
TEQFW_WEB__TYPE=http

# TLS values are required only for https mode.
# HTTPS TLS values must be supplied as a typed TEQFW_WEB configuration source.
